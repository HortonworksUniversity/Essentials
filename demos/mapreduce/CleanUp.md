# Clean Up Direction for [Foundational Processing with MapReduce](./README.md) Lab

Perform these clean up steps if the demo was previously run.

* as user `it1` run `hdfs dfs -rm -r -skipTrash avg-output`